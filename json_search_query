SELECT 
    *
FROM 
    orders
WHERE 
    JSON_UNQUOTE(JSON_EXTRACT(payload_data, '$.ORDER_DATE')) LIKE '1970-01-01%'
    OR JSON_UNQUOTE(JSON_EXTRACT(payload_data, '$.ORDER_DATE')) LIKE '0000-00-00%';
